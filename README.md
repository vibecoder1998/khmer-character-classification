# Khmer Character Classifier 🇰🇭

A Streamlit app for classifying Khmer handwritten or printed characters using a PyTorch model.

## Setup
```bash
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
